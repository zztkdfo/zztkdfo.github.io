---
layout: post
title:  "JPA 사용하기(2)"
date:   2020-02-22
comments: true
---
<br/>

이번 블로그에서는 앞에서 설명한 [JPA 사용하기(1)]에서 예제로 사용한 소스를 바탕으로 Spring Data JPA(Java Persistence API)에 대해 하나하나 자세히 알아 보도록 하겠습니다.
<br/>
### 엔티티 설정
- - -
```
@Getter
@Setter
@Entity
@Table(name ="TB_MEMBER",
    uniqueConstraints={
        @UniqueConstraint(
            name="INDEX_MEMBER_CODE",
            columnNames = {"MEMBER_CODE"}
        )
    }
)
public class Member {

    @Id
    @Column(name = "MEMBER_ID")
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "MEMBER_CODE", nullable = false)
    private String memberCode;

    @Column(name = "MEMBER_NAME")
    private String memberName;

    @Column(name = "MEMBER_AGE")
    private Integer memberAge;

    @JsonIgnore
    @ManyToOne
    @JoinColumn(name = "TEAM_ID")
    private Team team;

}
```
#### @Getter, @Setter
Getter, Setter 자동 생성 Lombok 라이브러리
#### @Entity
JPA를 사용해서 테이블과 매핑할 클래스에 @Entity 어노테이션을 필수로 붙여 사용한다.
#### @Table
 엔티티와 매핑할 테이블을 지정한다. @Table을 설정하지 않으면 엔티티명(Member)이름을 테이블 명으로 사용한다.



[JPA 사용하기(1)]: https://zztkdfo.github.io/posts/JPA-%EC%82%AC%EC%9A%A9%ED%95%98%EA%B8%B0(1)/
